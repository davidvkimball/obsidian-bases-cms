/**
 * Shared constants used across CMS views
 */

/** Default batch size for infinite scroll */
export const BATCH_SIZE = 50;

/** Gap size between elements (must match CSS) */
export const GAP_SIZE = 8;

